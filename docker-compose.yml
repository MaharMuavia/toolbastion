services:
  toolbastion:
    build:
      context: .
    image: toolbastion:local
    ports:
      - "127.0.0.1:4782:4782"
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
