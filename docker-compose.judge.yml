services:
  judge:
    image: ${TOOLBASTION_IMAGE:-ghcr.io/maharmuavia/toolbastion:v0.1.0}
    pull_policy: ${TOOLBASTION_PULL_POLICY:-always}
    ports:
      - "127.0.0.1:4782:4782"
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
