# Codex collaboration record

| Date | Module | How Codex accelerated work | Human decision or correction | Validation performed |
| --- | --- | --- | --- | --- |
| 2026-07-15 | Foundation | Inspected the build brief and environment, confirmed Codex structured-output CLI support, and created durable project/security records. | Human constrained v1 to one target and required deterministic checks before model judgment. | Verified Node, npm, Git, and Codex versions and inspected `codex exec --help`. |
| 2026-07-15 | MCP bridge | Scaffolded strict TypeScript workspaces, implemented a real SDK client/server stdio bridge and benign target, and stabilized Windows builds. | Human required stdout to remain protocol-only and child processes to use `shell: false`. | Clean `npm ci`; build, lint, and type-check passed; integration client listed and called `echo` through Warden; 3 tests passed. |
| 2026-07-15 | Deterministic security | Implemented policy validation, canonical path and symlink checks, SSRF and shell detectors, exact-call cache, persistent trust diffing, poisoned metadata detection, enforcement, and attack fixtures. | Human required deterministic checks before GPT and prohibited model overrides of hard denies. | 31 tests passed, including target-side proof that traversal was blocked before execution; CLI trust create/diff was clean; invalid YAML reported exact paths. |
| 2026-07-15 | Judge and observability | Securely provisioned a project API key, implemented structured parallel subchecks and offline replay, built the localhost API and responsive security dashboard, and verified safe model-failure handling. | Human chose GPT-5.6 and required honest offline labeling; API billing remains a human/account action. | Build, lint, type-check, and 40 tests passed; live request reached OpenAI and safely returned `ASK_USER` on account-inactive 429 with zero tokens. |
| 2026-07-15 | Output, audit, and remediation | Added result redaction/quarantine, a tamper-evident JSONL chain, deterministic reports, and a guarded Codex proposal workflow. Official Codex guidance and local CLI help determined the exact read-only invocation. | Human deferred all OpenAI API-key use; remediation strips `OPENAI_API_KEY`, never auto-applies, and requires explicit review. | Real `codex exec` returned schema-validated `NO_CHANGE`; patch tests accepted a narrow domain rule and rejected deny-rule removal. |
| 2026-07-15 | Release-candidate hardening | Built the full evaluation corpus, Attack Lab, static snapshot, browser suite, CI/release automation, and hardened container artifact. | Human kept the API key on hold, so every Day 5 gate remained offline and credential-free. | Clean install; lint/typecheck; 56 tests; 12 integration tests; 2 browser tests; 35/35 evaluation; zero audit vulnerabilities; read-only container health and download checks. |
