# Codex feedback session

Status: **NOT YET RECORDED — human action required**

The required `/feedback` Codex Session ID must come from the primary project task after the owner submits feedback in Codex. It cannot be inferred from a local path, Git commit, API key, or generated by the repository.

Before submission:

1. In the primary MCP Warden Codex task, run `/feedback`.
2. Submit the requested project feedback.
3. Copy the returned Codex Session ID.
4. Replace the status above with the exact ID.
5. Update the README, `SUBMISSION_CHECKLIST.md`, and `docs/submission-description.md` with the same ID.
6. Confirm the ID contains no credential or private message content.
