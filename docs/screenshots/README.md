# Submission screenshots

These images are captured from the real built application using the committed offline fixture session. They contain no credentials and must retain the `OFFLINE FIXTURE REPLAY` label.

- `dashboard-overview.png`: runtime metrics, critical event, and dashboard boundary.
- `attack-lab-block.png`: path-traversal expected/actual `BLOCK` result.

Before publication, inspect both at full resolution for private notifications or environment details. Do not substitute live-model claims into these recorded screenshots.
