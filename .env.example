# Live judge mode only. Never commit a populated .env file.
OPENAI_API_KEY=
TOOLBASTION_MODEL=gpt-5.6

# Required when the dashboard binds beyond localhost. Use a unique base64url secret (32-256 characters).
TOOLBASTION_API_TOKEN=
